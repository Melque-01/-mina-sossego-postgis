# Copie para .env e preencha — NUNCA commite .env real
# Exemplo dev local (VM descartável). Em produção use senha forte e não commite.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=sossego_desmate
DB_USER=ubunto
DB_PASS=dev_only_change_me

# DSN completo (opcional — se definido, sobrescreve os campos acima)
# DB_DSN=host=localhost dbname=sossego_desmate user=ubunto password=dev_only_change_me
# DB_DSN_FALLBACK=host=localhost port=15432 dbname=sossego_desmate user=ubunto password=dev_only_change_me

ENV=development
# ENV=production
